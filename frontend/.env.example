# SokoAgent AI assistant (required for /api/agent)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# The dedicated Celo wallet address for the SokoAgent (used for 8004.io + Self Agent ID registration)
AGENT_WALLET_ADDRESS=0x...

# Contract addresses are in lib/contracts.ts (already configured for Celo mainnet).
# To run locally:
#   cd frontend && npm install && npm run dev
